server:
  port: 8080
spring:
  application:
    name: eureka-server # ������������

eureka:
  client:
    register-with-eureka: false # �Լ���ע��
    fetch-register: false # ���ü�������
    service-url:
      defaultZone: http://localhost:8080/eureka